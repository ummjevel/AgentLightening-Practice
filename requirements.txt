# arXiv Paper Summarizer - Python Dependencies
#
# Install with: pip install -r requirements.txt
#

# Core dependencies
arxiv==2.3.1
PyYAML==6.0.1
requests==2.32.5

# PDF processing
PyMuPDF==1.26.6           # PDF text extraction and processing
Pillow==12.0.0            # Image processing and manipulation

# LLM and AI
openai==2.8.1             # OpenAI-compatible API client (for SKT-AI A.X-4.0)

# Template rendering
Jinja2==3.1.6             # HTML template engine

# Utilities
python-dateutil==2.9.0    # Date/time utilities
tqdm==4.67.1              # Progress bars

# Agent Lightning for optimization (optional)
# Uncomment the line below to enable Agent Lightning tracking features
# Note: This is optional and the system works fine without it
# agentlightning>=0.1.0

# Additional dependencies installed automatically:
# - feedparser (via arxiv)
# - anyio, httpx, pydantic (via openai)
# - MarkupSafe (via Jinja2)
